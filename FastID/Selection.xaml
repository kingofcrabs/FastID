<Window x:Class="FastID.SelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Selection" Height="300" Width="300">
    <Grid>
        <StackPanel>
            <Label>配置文件：</Label>
            <ListBox Name="lstConfig" ItemsSource="{Binding}">
                
            </ListBox>
            <Button Name="btnConfirm" Click="btnConfirm_Click" Margin="100,0">确定</Button>
            
        </StackPanel>
    </Grid>
</Window>
